package co.simplon.jamixbusiness.mappers;

public class OfferMapper {

    /*
     * public static Set<OfferViewDto> entitiesToDtos(Set<Offer> offers) { return
     * offers.stream().map(o -> new OfferViewDto(o.getId(), o.getTitle(),
     * o.getCity(), o.getZipCode(), o.getImageId(), o.getInstrument() != null ?
     * o.getInstrument().getId() : null, o.getStyle() != null ? o.getStyle().getId()
     * : null, o.getGoal() != null ? o.getGoal().getId() : null, o.getDescription(),
     * o.getContactMail(), o.getCreatedAt())).collect(Collectors.toSet()); }
     * 
     * public static Set<Offer> dtosToEntities(Set<OfferViewDto> dtos,
     * InstrumentRepository instrumentRepo, StyleRepository styleRepo,
     * GoalRepository goalRepo) { return dtos.stream().map(dto -> { Offer offer =
     * new Offer(); offer.setTitle(dto.title()); offer.setCity(dto.city());
     * offer.setZipCode(dto.zipCode()); offer.setImageId(dto.imageId()); return
     * offer; }).collect(Collectors.toSet()); }
     */
}
